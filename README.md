# JoseLuisProject
Bot de Telegram


dependencias
-una vez clonado desde vscode vete a la terminal y escribe

`npm init -y`
`npm install node-telegram-bot-api`
INSTALA PYTHON PARA USAR EL LECTOR DE IMAGENES
luego pon cada linea una por una en el terminal
`python -m venv venv
.\venv\Scripts\activate
pip install -r .\requirements.txt`

se inicia con nodemon para hacer cambios y funcione inmediatamente, pero no es necesario
`npm install -g nodemon` y una vez descargues todo se corre con `nodemon bot.js`

